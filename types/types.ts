// types.ts
export interface TestimonialProps {
    name: string;
    country: string;
    feedback: string;
    image: string;
  }
  
  // types.ts
export interface WhyChooseUsProps {
  title: string;
  description: string;
  buttonText: string;
  onButtonClick: () => void;
}
